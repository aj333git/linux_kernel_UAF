Kernel UAF (Use-After-Free) Detector Module using KASAN-style Canary Validation

This complements your allocator by detecting:

stale pointer access
double free
invalid memory touch after reclaim
delayed RCU misuse
freelist corruption

Kernel UAF (Use-After-Free) Detector Module using KASAN-style Canary Validation

This complements your allocator by detecting:

stale pointer access
double free
invalid memory touch after reclaim
delayed RCU misuse
freelist corruption

It is fully runnable as a standalone external module.

This module focuses on:

runtime memory safety validation
temporal memory bugs
post-free access detection

This is how production kernels catch:

UAF exploits
dangling pointers
race-triggered corruption



Features

This independent module includes:

custom slab cache
canary validation
delayed RCU freeing
use-after-free detection
double-free detection
procfs runtime report
timer-driven corruption checks
freelist poisoning
stack traces
trace_printk observability
kernel-space only



🧠 What is happening (step-by-step)
1. Allocation worked
ALLOC id=1

✔ Your kmem_cache_alloc() + object setup is fine.

2. UAF detection triggered
USE_AFTER_FREE id=1

✔ Your logic correctly detected:

if (obj->freed)

So your UAF simulation is working correctly.

3. The FREE log is corrupted
FREE id=-1515870811

🚨 This is the important problem.

That number:

-1515870811 ≈ 0xA5A5A5A5 pattern corrupted


insmod  → module loads
cat /proc → view stats
trace_pipe → observe kernel events
rmmod → unload module


